# Result records are regenerated by every run; the committed evidence lives in
# expected/ once a scenario's result is pinned. actual/ is scratch.
actual/
control_delivery_eval/.venv/
control_delivery_eval/out/
.venv/
__pycache__/
